# Fabrik — Registro de Carga (app)
Ferramenta interna provisória de registro de carga da Fabrik. Os dados ficam no Supabase,
protegidos por login + RLS — a chave `anon` neste código é pública por design.
Repo de deploy (GitHub Pages); o projeto completo (SQL, docs) vive fora.
